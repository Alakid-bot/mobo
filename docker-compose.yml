services:
  bot:
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/data
    ports:
      - "8080:8080"
    environment:
      DB_PATH: /data/mobo.db
      PORT: 8080
      COOKIE_SECURE: "false"
